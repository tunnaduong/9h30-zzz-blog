media:
  input: src/assets/images
  output: ./assets
content:
  - name: posts
    type: collection
    path: src/data/blog
    fields:
      - name: title
        label: Title
        type: string
      - name: author
        label: Author
        type: string
      - name: description
        label: Description
        type: text
      - name: draft
        label: Is draft?
        type: boolean
        default: true
      - name: featured
        label: Is featured?
        type: boolean
        default: false
      - name: sections
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{title} ({index})"
        fields:
          - name: title
            type: string
      - name: tags
        label: Tags
        type: select
        options:
          multiple: true  # Cho phép chọn nhiều tag cùng lúc
          create: true    # CHO PHÉP TỰ GÕ VÀO TAG MỚI nếu không có trong danh sách
          values:         # Danh sách các tag gợi ý sẵn có
            - Công nghệ
            - Lập trình
            - Chuyện đời
            - Linh tinh
            - Tâm sự mỏng
      - name: pubDatetime
        label: Published Datetime
        type: date
        options: 
          time: true
          format: "yyyy-MM-dd'T'HH:mm:ss'Z'"
      - name: heroImage
        label: Hero Image
        type: image
      - name: body
        label: Body
        type: rich-text
    view:
      fields: [title, description, pubDatetime, heroImage]